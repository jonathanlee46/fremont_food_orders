var app = app || {};
var ENTER_KEY = 13;
var ESC_KEY = 27;

$(function () {
	'use strict';

	//create the app
  // var bill = new Todos();
  // var order = new app.Todo();

	var app = new app.AppView();
});
